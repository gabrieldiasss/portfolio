import { styled } from "../../../../styles";

export const TechnologiesCardContainer = styled("div", {
  background: "#fff",
  padding: "2rem",
  borderRadius: 4,
  boxShadow: "$default",
  cursor: "pointer",
});
