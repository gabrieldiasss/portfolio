import { styled } from "../../../styles"

export const CardProjectSecondaryContainer = styled("div", {
    display: "flex",
    img: {
        borderRadius: 8,
    },
    gap: "1rem"
})

export const CardProjectSecondaryContent = styled("div", {
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between"
})